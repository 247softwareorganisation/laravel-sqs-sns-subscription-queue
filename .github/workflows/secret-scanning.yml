name: Secret Scanning

on:
  pull_request:
    branches: [master]

concurrency:
  group: secret-scanning-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  secret-scan:
    uses: 247softwareorganisation/github-workflows/.github/workflows/secret-scanning.yml@master
